# AWS Cognito Configuration
# Get these values from your AWS Cognito User Pool

# Auth Provider
# AUTH_PROVIDER=cognito

# AWS Region where your Cognito User Pool is located
AWS_REGION=eu-north-1

# Cognito User Pool ID (found in AWS Console > Cognito > User Pools > Your Pool > Pool details)
COGNITO_USER_POOL_ID=eu-north-1_xxxxxxxxx

# Cognito App Client ID (found in AWS Console > Cognito > User Pools > Your Pool > App integration > App clients and analytics)
COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx

# Cognito App Client Secret (only required if your app client has a secret configured)
# If your app client doesn't have a secret, you can remove this line
COGNITO_CLIENT_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: Application Configuration
# NODE_ENV=development
# PORT=3000
